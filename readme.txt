We will compare to all existing work:

numaPerf: has to compare with numaProf, SNPERF (An application-centric ccNUMAmemory profiler), NUMAgrind, Numatop, HPCToolkit (Xu's work). Basic idea is to check the memory sharing pattern. 

MemProf: a Memory Profiler for NUMA Multicore Systems (ATC'12)
